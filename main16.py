# main.py
# Import the user-defined module
import math

# Take input from user
a = float(input("Enter first number: "))
b = float(input("Enter second number: "))

# Use functions from module
print("Addition:", math.add(a, b))
print("Subtraction:", math.subtract(a, b))
print("Multiplication:", math.multiply(a, b))
print("Division:", math.divide(a, b))
